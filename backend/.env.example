OMDB_API_KEY=
PORT=3001
# Postgres connection string (e.g. from Vercel Postgres or Neon).
# Vercel injects this automatically in production once you connect a
# database to the project; for local dev, paste the same value here.
POSTGRES_URL=
