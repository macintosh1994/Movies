{
  "name": "have-you-actually-seen-it",
  "private": true,
  "type": "module",
  "scripts": {
    "vercel-build": "npm --prefix frontend install && npm --prefix frontend run build"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "pg": "^8.13.0"
  }
}
